 # 🧪 Автоматизация логина на SauceDemo (Playwright + Allure)

Проект содержит сценарии автоматизированного тестирования авторизации для сайта [SauceDemo](https://www.saucedemo.com). Тесты написаны на Python с использованием Playwright и запускаются в изолированном Docker-контейнере.

 ## 💻 Системные требования
* **ОС:** Windows 10 Pro (22H2) и выше.
* **IDE:** PyCharm 2025.1.1.1 Build #PY-251.25410.159.
* **Docker Desktop:** Версия 4.40.0+ (с установленным Engine 28.0+).
* **Allure CLI:** Для генерации и просмотра отчетов на локальной машине.

---

 ## 🐳 Запуск в Docker (для Windows)

Все действия выполняются в терминале **PowerShell**, открытом в папке проекта.
Папка с результатами отчетов Allure-results очищается автоматически.

 ### 1. Сборка Docker-образа
Эта команда создаст образ со всеми необходимыми зависимостями (Python 3, Playwright, браузеры).
```powershell
docker build -t sauce-tests .
```
 
 ### 2. Запуск тестов
Запускает процесс тестирования и синхронизирует результаты с папкой проекта. Флаг --rm удалит временный контейнер после работы, а ключ -v пробросит отчеты в проект:
```powershell
Remove-Item -Recurse -Force allure-results; docker compose up --build --exit-code-from test_runner
```
 ### 3. Очищаем ресурсы compose
```powershell
docker compose down
```

 ## 🚀 Локальный запуск

Все действия выполняются в терминале **PowerShell**, открытом в папке проекта.
 
 ### 1. Запуск тестов c формированием отчета
Запускает процесс тестирования c формированием отчета:
```powershell
pytest --clean-alluredir
```


 ### 2. Запуск тестов без очистки директории
Запускает процесс тестирования в headed режиме:
```powershell 
pytest --headed
```

Запускает процесс тестирования в headless режиме по умолчанию:
```powershell 
pytest
```

## 📈 Просмотр результатов тестирования
После завершения тестов открытие отчета Allure в браузере (требуется установленный Allure CLI):
```powershell
allure serve allure-results
```


🛠 Стек технологий проекта
```
Базовый образ: mcr.microsoft.com/playwright:v1.41.0-jammy
Python: 3.10+ (используется версия 3.10.11)
Библиотеки:
pytest==8.0.0
playwright==1.41.0
pytest-playwright==0.4.4
allure-pytest==2.13.2
```

## 🔒 Безопасность и конфигурация
``` 
В текущей версии учетные данные хранятся в `data/test_data.py`. 
Для промышленной эксплуатации рекомендуется использовать переменные окружения:
1. Установить библиотеку: pip install python-dotenv.
2. Создать файл `.env` в корне проекта:

SAUCE_PASSWORD=secret_sauce
STANDARD_USER=standard_user
LOCKED_USER=locked_out_user
GLITCH_USER=performance_glitch_user

3. Добавить .env в файл .gitignore.

4. В файле test_data.py настроить получение данных через os.getenv():

import os
from dotenv import load_dotenv

load_dotenv()

class Users:
    STANDARD_USER = os.getenv("STANDARD_USER", "standard_user")
    LOCKED_USER = os.getenv("LOCKED_USER", "locked_out_user")
    GLITCH_USER = os.getenv("GLITCH_USER", "performance_glitch_user")
    PASSWORD = os.getenv("SAUCE_PASSWORD", "secret_sauce")


Это позволит легко менять стенды (Dev/Prod) и безопасно хранить пароли в CI/CD (через GitHub Secrets).
```

